{
  "name": "xmlconfigupdate",
  "version": "0.0.1",
  "description": "build helper that updates a web config xml file to point to a new deployment folder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Roth",
  "license": "ISC",
  "dependencies": {
    "@xmldom/xmldom": "^0.8.10"
  }
}
